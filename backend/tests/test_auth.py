"""Tests for authentication endpoints — BYPASSED for now."""

from __future__ import annotations


# Auth tests are disabled while authentication is bypassed.
# They will be re-added when the backend switches to a database.
